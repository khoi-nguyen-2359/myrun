project.ext {
    localProps = new Properties()
}

File localPropsFile = project.rootProject.file('local.properties')
if (localPropsFile.exists()) {
    project.localProps.load(localPropsFile.newDataInputStream())
}
